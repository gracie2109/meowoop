export const iconPets = Array.from({ length: 21 }, (_, i) => `pets_${i + 1}`)

export const iconNature = Array.from({ length: 1 }, (_, i) => `nature_${i + 1}`)

export const iconCircleArrowLeft = `common_circle-arrow-left`
